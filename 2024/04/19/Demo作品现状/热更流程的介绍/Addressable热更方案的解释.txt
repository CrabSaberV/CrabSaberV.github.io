大体设计流程：
打包->远端仓库->生成md5列表（用于验证完整性）。
本地启动->是否可连接到远端->下载远端md5列表->与本地md5列表比较->获取远端需要下载的bundle列表->下载所需的bundle包 ->继续游戏流程

最初考虑本地不保存md5列表，因为考虑到一些特殊情况本地md5列表和本地bundle包不匹配。
但是随着项目资源量的增加，热更检查遍历每个bundle包并检查md5是非常缓慢的，于是决定本地缓存md5列表。

对于遍历本地bundle包进行检查的方案，可以作为热更后游戏异常的修复手段。
